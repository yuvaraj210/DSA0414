import pandas as pd
from sklearn.cluster import KMeans

df = pd.read_csv("retail.csv")
X = df[["amount_spent","visit_freq"]]

kmeans = KMeans(n_clusters=2)
df["Cluster"] = kmeans.fit_predict(X)

print(df)
