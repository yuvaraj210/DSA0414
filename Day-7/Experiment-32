import pandas as pd
from sklearn.linear_model import LinearRegression
from sklearn.metrics import r2_score

df = pd.read_csv("houses_lr.csv")

X = df[["size"]]
y = df["price"]

model = LinearRegression()
model.fit(X,y)

pred = model.predict(X)

print("R2 Score:", r2_score(y,pred))
